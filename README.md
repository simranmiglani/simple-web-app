# Simple Web App (React + Express)

A tiny full-stack demo app:
- React (Vite) client
- Express API server
- Fetch list of items + add a new item

## Prereqs
- Node.js 18+

## Setup

### Server
```bash
cd server
npm install
cp .env.example .env
npm run dev
